'use client';

import { motion } from 'framer-motion';
import { typingText } from '../c'

import styles from '../styles';
import {  slideIn, staggerContainer, textVariant } from '../utils/motion';

const About = () => (
  <section>
    About sectionhcd
  </section>
);

export default About;
